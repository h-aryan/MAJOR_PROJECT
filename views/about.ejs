<% const renderBody=()=> { %>
    <div class="container">
        <h1>About</h1>
        <p>We list properties and route qualified leads to brokers. AI qualification calls happen outside this site.</p>
    </div>
    <% }; %>
        <%- include('layout', { title, includeBody: renderBody }) %>